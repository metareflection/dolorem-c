(include "def.dlr")

(defun hello () void (progn
  (var x int)
  (assign x 5)
  (:= y 10)
  (cond y
    (puts "yay"))
  (printf "%i %i\n" x y)))
(defmacro run (progn (hello) (make-cexp "" "" "" "")))
(run)
